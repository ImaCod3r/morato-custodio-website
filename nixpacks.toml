# Configuração de build/arranque para o Dokploy (Nixpacks).
# A aplicação é servida pelo `vite preview` na porta 3005.

[phases.setup]
nixPkgs = ["nodejs_24"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"

[variables]
PORT = "3005"
